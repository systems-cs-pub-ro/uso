#include <stdio.h>
#include "inc.h"

int main()
{
    const double pi = PI;
    const double euler = E;
    printf("pi is equal to %f\n", pi);
    printf("Euler's number is %f\n", euler);
    return 0;
}
